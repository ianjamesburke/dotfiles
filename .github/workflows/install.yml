name: install

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5

      - name: Install Homebrew (Linux)
        if: runner.os == 'Linux'
        run: |
          /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          echo "/home/linuxbrew/.linuxbrew/bin" >> "$GITHUB_PATH"

      - name: Skip macOS-only formulae (Linux)
        if: runner.os == 'Linux'
        run: echo "HOMEBREW_BUNDLE_BREW_SKIP=trash" >> "$GITHUB_ENV"

      - name: Trust third-party taps
        run: brew trust satococoa/tap

      - name: brew bundle
        run: brew bundle install --file=Brewfile --no-upgrade

      - name: zsh syntax check
        run: zsh -n zshrc
